{
  "name": "deck",
  "version": "1.0.0",
  "description": "Math lecture slides powered by Marp",
  "private": true,
  "scripts": {
    "tikz": "bash scripts/build-tikz.sh",
    "dev:tikz": "chokidar 'tikz/**/*.tex' -c 'npm run tikz'",
    "dev:marp": "marp --watch --html --input-dir slides/",
    "dev": "npm run tikz && concurrently 'npm run dev:tikz' 'npm run dev:marp'",
    "build": "npm run tikz && marp --html --input-dir slides/ --output dist/",
    "pdf": "npm run tikz && marp --html --pdf --input-dir slides/ --output dist/"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^9.2.1"
  }
}
